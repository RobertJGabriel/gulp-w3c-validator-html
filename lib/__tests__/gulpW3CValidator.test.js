const assert = require('assert');
const gulpW3CValidator = require('../index.js');

describe('gulpW3CValidator', function () {
  it('has a test', function () {
    assert(false, 'gulpW3CValidator should have a test');
  });
});
